{
  "name": "neo-quick-poll",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ neo-quick-poll",
    "create-local": "graph create --node http://localhost:8020/ neo-quick-poll",
    "remove-local": "graph remove --node http://localhost:8020/ neo-quick-poll",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 neo-quick-poll",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.85.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
